# ISORYN Engine

ISORYN is a new game-engine program built from a clean repository with Godot as an evaluated foundation and with HIVE, CORE and IRIS as architectural reference systems.

The project is governed by GEF and is HIVE-first. Product implementation is not yet authorized; the current phase is repository bootstrap, source-pack establishment, HIVE/GEF integration and architecture discovery.

## Local canonical workspace

`D:\\Hive\\Projects\\isoryn-engine`

Machine-specific paths are documentation only and must not be hard-coded into portable runtime contracts.

## Engineering lifecycle

`ANALYZE -> SOURCE CHECK -> NEXT NECESSARY INCREMENT -> WORK ORDER -> CONTEXT LOCK -> PREFLIGHT -> EXECUTOR -> TESTS/EVIDENCE -> PR -> AUDIT -> VERDICT -> CHECKPOINT DELTA -> MERGE -> NEXT`

Read `AGENTS.md`, `.engineering/SOURCE-HIERARCHY.md` and `docs/project-brain/13-CHECKPOINT.md` before governed work.

## Current status

BOOTSTRAP ACTIVE. GEF/HIVE scaffolding is being established before engine implementation.
