# Implement admitted ISORYN Work Order

Before editing:
1. resolve Git root, branch, HEAD and cleanliness;
2. read AGENTS.md and .engineering/SOURCE-HIERARCHY.md;
3. read canonical checkpoint and the active Work Order;
4. run HIVE-first preflight through the configured MCP when available;
5. confirm Context Lock/base is current;
6. execute only the admitted scope.

Run required tests, correct causal failures introduced by the work, produce exact-head evidence, commit/push/update PR, and stop at the Work Order STOP CONDITION. Final review/report in Brazilian Portuguese.
